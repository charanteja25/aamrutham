# No Razorpay keys needed here — the backend handles them.
# In production, set VITE_API_URL if your backend is on a different domain.
# VITE_API_URL=https://api.yourdomain.com
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx